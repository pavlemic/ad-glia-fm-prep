# =============================================================================
# ad-glia-fm-prep — scGPT env (Option-B env split, 2026-07-03)
#
# Used by: the scGPT zero-shot + CPT/LoRA notebooks (colab_10, and the scGPT
# regimes of colab_11+). scGPT 0.2.5 pins scvi-tools<1.0 and an older stack
# (scanpy<2, etc.), which requires Python <3.11 -> this env needs a Python-3.10
# runtime on Colab (kernel swap or a py310 conda env), UNLIKE the Geneformer
# env which runs on native Python (see requirements_geneformer.txt).
#
# This file was the former single "requirements_fm.txt"; it was split so that
# Geneformer (no scvi-tools/scgpt dep, native Python) is no longer coupled to
# scGPT's 3.10 constraint. DO NOT install in the same env as
# requirements_geneformer.txt or requirements_integration.txt.
#
# Hand-off from integration env: h5ad files written by the integration env,
# read by this env.
# =============================================================================

# Stable bottom — scGPT-constrained (scanpy <2, pandas <2 historically, etc.)
scanpy==1.9.8                  # scGPT pins <2.0.0
anndata==0.10.9
leidenalg==0.10.2
igraph==0.11.6
umap-learn==0.5.6
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
matplotlib==3.9.2
seaborn==0.13.2
scikit-learn==1.5.2
jupyter==1.1.1
ipykernel==6.29.5

# Hard-pinned to resolve the scGPT × scvi-tools conflict
scvi-tools==0.20.3             # last pre-1.0; required by scGPT 0.2.5
scgpt==0.2.5

# Transformers + adapters stack (TBD — resolved at first scGPT Colab install,
# then pinned via `!pip freeze`). Known hazards:
#   - peft must match transformers minor
#   - accelerate must match transformers minor
#   - huggingface_hub must match transformers
# Stubs:
# transformers==TBD
# huggingface_hub==TBD
# peft==TBD
# accelerate==TBD
# datasets==TBD               # scGPT pins <3

# torch: TBD — match Colab CUDA wheel. Range required: >=2.2 if flash-attn
# is wanted (scGPT `fa2` extras), else >=1.13 to satisfy scGPT base.
# torch==TBD

# flash-attn: TBD — CUDA-pinned wheel. Optional; install only after torch
# is pinned. scGPT `fa2` group requires >=2.8.
# flash-attn==TBD
